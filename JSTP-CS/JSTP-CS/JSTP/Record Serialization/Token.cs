namespace Jstp.Rs {
	enum Token {
		TCurlyOpen = 0,
		TCurlyClose,
		TBracketOpen,
		TBracketClose,
		TString,
		TNumber,
		TTrue,
		TFalse,
		TNull,
		TUndefined,
		TKey,
		TColon,
		TComma,
		TNone
	}
}
